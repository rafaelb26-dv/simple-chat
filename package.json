{
  "name": "simple-chat",
  "version": "1.0.8",
  "description": "a simple chat aplication",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "docker-image": "docker build --no-cache -t $npm_package_name .",
    "docker-container": "docker run -t -p 3000:3000 -d $npm_package_name"
  },
  "author": "Rafael B. De Sousa",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.0",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "http": "^0.0.1-security",
    "mongoose": "^6.5.3",
    "socket.io": "^4.5.1"
  },
  "devDependencies": {
    "jasmine": "^4.3.0",
    "request": "^2.88.2"
  }
}
